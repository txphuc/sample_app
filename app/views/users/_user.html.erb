<li>
  <%= gravatar_for user, size: 50 %>
  <%= link_to user.name, user %>
  <% if can_delete? user.id %>
    |
    <%= link_to t(".delete"), user, method: :delete,
                data: { confirm: t(".delete_confirm") }
    %>
  <% end %>
</li>
